/**
 * Bootstrap-only application root. B02 replaces this with the command
 * workspace once the design-system batch begins.
 */
export function App() {
  return (
    <main className="bootstrap" aria-label="Battle Builder">
      <p>Battle Builder is preparing the board planner.</p>
    </main>
  );
}
