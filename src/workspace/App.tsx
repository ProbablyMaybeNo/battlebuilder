/* eslint-disable react-hooks/exhaustive-deps */
import { lazy, Suspense, useEffect, useMemo, useState } from 'react';
import { Box, ChevronDown, CircleHelp, Copy, Crosshair, FilePlus2, FolderOpen, Grid3X3, Hammer, Layers3, Link2, LockKeyhole, Maximize2, Minus, MousePointer2, Plus, Redo2, Save, Settings2, Trash2, Undo2, Upload } from 'lucide-react';
import { createHistory, commit, redo, undo, type History } from '../document/history';
import { newBoard, parseBoard, starterBoard, type BoardDocument, type Piece, type PieceKind, type WallSide } from '../document/schema';
import { catalog, catalogByKind } from '../model/catalog';
import { addAccess, createPiece, joinStructures, movePieces, normalizeRotation, placementResult, removeAccess, resizePiece } from '../model/board-operations';
import { OverheadBoard, type BoardMode } from '../renderer/overhead';
const ThreeBoard = lazy(async () => ({ default: (await import('../renderer/three-board')).ThreeBoard }));
import { Button, Dialog, Drawer, IconButton, Menu, MenuItem, NumberField, Popover, Tabs, ToastRegion, type Toast } from './components';

type DrawerId = 'board' | 'build' | 'layers' | 'setup';
type AccessType = 'door' | 'window';
const drawerMeta = [{ id: 'board' as const, label: 'Board', icon: Box }, { id: 'build' as const, label: 'Build', icon: Hammer }, { id: 'layers' as const, label: 'Layers', icon: Layers3 }, { id: 'setup' as const, label: 'Setup', icon: Settings2 }];

function SelectionInspector({ piece, selectionCount, onClose, onChange, onDelete, onDuplicate, onJoin, joinReason, onAccessType, accessType, onRemoveAccess }: { piece: Piece | null; selectionCount: number; onClose: () => void; onChange: (change: Partial<Piece>) => void; onDelete: () => void; onDuplicate: () => void; onJoin: () => void; joinReason: string | null; onAccessType: (type: AccessType) => void; accessType: AccessType; onRemoveAccess: (id: string) => void }) {
  const [tab, setTab] = useState('properties'); if (!piece) return null;
  const structure = piece.structureDetails;
  const setDimensions = (key: 'width' | 'height', value: number) => onChange(resizePiece(piece, key === 'width' ? value : piece.width, key === 'height' ? value : piece.height));
  const properties = <div className="inspector-fields"><p>{selectionCount > 1 ? `${selectionCount} objects selected. Numeric edits apply to the active structure.` : catalogByKind(piece.kind).description}</p><div className="field-grid"><NumberField label="X (in)" value={piece.x} min={0} max={71} onCommit={(x) => onChange({ x: Math.round(x) })} /><NumberField label="Y (in)" value={piece.y} min={0} max={71} onCommit={(y) => onChange({ y: Math.round(y) })} /><NumberField label="Width (in)" value={piece.width} min={catalogByKind(piece.kind).bounds.minWidth} max={catalogByKind(piece.kind).bounds.maxWidth} onCommit={(width) => setDimensions('width', width)} /><NumberField label="Height (in)" value={piece.height} min={catalogByKind(piece.kind).bounds.minHeight} max={catalogByKind(piece.kind).bounds.maxHeight} onCommit={(height) => setDimensions('height', height)} /><NumberField label="Rotation" value={piece.rotation} min={0} max={359} onCommit={(rotation) => onChange({ rotation: normalizeRotation(rotation) })} /></div><div className="inspector-actions"><Button variant="quiet" onClick={() => onChange({ locked: !piece.locked })}><LockKeyhole size={14} />{piece.locked ? 'Unlock' : 'Lock'}</Button><Button variant="quiet" onClick={() => onChange({ hidden: !piece.hidden })}>{piece.hidden ? 'Show' : 'Hide'}</Button><Button variant="quiet" onClick={onDuplicate}><Copy size={14} />Duplicate</Button><Button variant="danger" onClick={onDelete}><Trash2 size={14} />Delete</Button></div>{selectionCount > 1 && <div className="join-block"><Button variant="accent" onClick={onJoin} disabled={Boolean(joinReason)}><Link2 size={14} />Join structures</Button>{joinReason && <small>{joinReason}</small>}</div>}</div>;
  const structureContent = structure ? <div className="inspector-fields"><div className="field-grid"><NumberField label="Height (in)" value={structure.heightInches} min={1} max={72} onCommit={(heightInches) => onChange({ structureDetails: { ...structure, heightInches: Math.round(heightInches) } })} /><NumberField label="Elevation (in)" value={structure.elevationInches} min={-72} max={72} onCommit={(elevationInches) => onChange({ structureDetails: { ...structure, elevationInches: Math.round(elevationInches) } })} /></div><Button variant="quiet" onClick={() => onChange({ structureDetails: { ...structure, roofMode: structure.roofMode === 'roof' ? 'interior' : 'roof' } })}>Roof treatment: {structure.roofMode === 'roof' ? 'Roof' : 'Interior'}</Button><p className="muted">Direct resize and rotation handles are available in Select mode.</p></div> : <p>This terrain has no structure details.</p>;
  const access = structure && piece.kind !== 'platform' ? <div className="inspector-fields"><p>Choose a feature, then use Access mode and click an exterior wall. Features snap to one-inch wall cells.</p><div className="segmented"><Button variant={accessType === 'door' ? 'accent' : 'quiet'} onClick={() => onAccessType('door')}>Door</Button><Button variant={accessType === 'window' ? 'accent' : 'quiet'} onClick={() => onAccessType('window')}>Window</Button></div>{[['Doors', structure.doors], ['Windows', structure.windows]].map(([label, features]) => <div className="access-list" key={label as string}><strong>{label as string}</strong>{(features as NonNullable<typeof structure>['doors']).length ? (features as NonNullable<typeof structure>['doors']).map((feature) => <div key={feature.id}><span>{feature.wall} · {feature.offset + 1}–{feature.offset + feature.span} in</span><Button variant="quiet" onClick={() => onRemoveAccess(feature.id)}>Remove</Button></div>) : <small>None</small>}</div>)}</div> : <p>Doors and windows are supported on buildings and ruins only.</p>;
  return <aside className="inspector" aria-label={`${piece.name} inspector`}><div className="inspector__header"><div><p className="eyebrow">Selected terrain</p><h2>{piece.name}</h2></div><IconButton label="Close inspector" onClick={onClose}>×</IconButton></div><Tabs selected={tab} onSelect={setTab} tabs={[{ id: 'properties', label: 'Properties', content: properties }, ...(structure ? [{ id: 'structure', label: 'Structure', content: structureContent }, { id: 'access', label: 'Access', content: access }] : []), { id: 'notes', label: 'Notes', content: <p>{piece.notes || 'No notes yet.'}</p> }]} /></aside>;
}

export function App() {
  const [history, setHistory] = useState<History>(() => createHistory(newBoard())); const board = history.present;
  const [activeDrawer, setActiveDrawer] = useState<DrawerId | null>(null); const [mode, setMode] = useState<BoardMode>('neutral'); const [snap, setSnap] = useState(true); const [controlsOpen, setControlsOpen] = useState(false); const [helpOpen, setHelpOpen] = useState(false); const [selectedIds, setSelectedIds] = useState<string[]>([]); const [hoveredPieceId, setHoveredPieceId] = useState<string | null>(null); const [toasts, setToasts] = useState<Toast[]>([]); const [selectedCatalog, setSelectedCatalog] = useState<PieceKind>('building'); const [accessType, setAccessType] = useState<AccessType>('door'); const [view, setView] = useState<'overhead' | '3d'>('overhead'); const [preset, setPreset] = useState<'top' | 'isometric' | 'perspective' | 'front'>('isometric');
  const announce = (message: string, tone: Toast['tone'] = 'info') => setToasts((items) => [...items.slice(-2), { id: Date.now(), tone, message }]);
  const mutate = (next: BoardDocument) => setHistory((current) => commit(current, parseBoard(next)));
  const selectedPiece = selectedIds.length ? board.pieces.find((piece) => piece.id === selectedIds.at(-1)) ?? null : null;
  const toggleDrawer = (id: DrawerId) => setActiveDrawer((current) => current === id ? null : id); const groupedCatalog = useMemo(() => catalog.reduce<Record<string, typeof catalog>>((groups, item) => ({ ...groups, [item.group]: [...(groups[item.group] ?? []), item] }), {}), []);
  const command = (next: BoardDocument, message: string, selection: string[] = selectedIds) => { mutate(next); setSelectedIds(selection); announce(message, 'success'); };
  const addBuild = (kind: PieceKind, x: number, y: number, width: number, height: number) => { const piece = createPiece(board, kind, x, y, width, height); const result = placementResult(board, piece); if (!result.ok) { announce(`Cannot place ${catalogByKind(kind).singular.toLowerCase()}: ${result.reason}.`, 'warning'); return; } command({ ...board, pieces: [...board.pieces, piece] }, `${piece.name} built at ${x}, ${y}.`, [piece.id]); setMode('select'); };
  const move = (ids: string[], dx: number, dy: number) => { const result = movePieces(board, ids, dx, dy); if (!result.result.ok) { announce(`Move cancelled: ${result.result.reason}.`, 'warning'); return; } command({ ...board, pieces: result.pieces }, `Moved ${ids.length} ${ids.length === 1 ? 'object' : 'objects'}.`, ids); };
  const resize = (id: string, width: number, height: number) => { const original = board.pieces.find((piece) => piece.id === id); if (!original) return; const piece = resizePiece(original, width, height); const result = placementResult(board, piece, [id]); if (!result.ok) { announce(`Resize cancelled: ${result.reason}.`, 'warning'); return; } command({ ...board, pieces: board.pieces.map((entry) => entry.id === id ? piece : entry) }, `${piece.name} resized to ${piece.width} × ${piece.height} in.`, [id]); };
  const patchPiece = (change: Partial<Piece>) => { if (!selectedPiece) return; const piece = { ...selectedPiece, ...change }; const result = placementResult(board, piece, [piece.id]); if (!result.ok && !change.hidden && !change.locked) { announce(`Change cancelled: ${result.reason}.`, 'warning'); return; } command({ ...board, pieces: board.pieces.map((entry) => entry.id === piece.id ? piece : entry) }, `${piece.name} updated.`, [piece.id]); };
  const duplicate = () => { if (!selectedPiece) return; for (let y = 0; y <= board.settings.heightInches - selectedPiece.height; y += 1) for (let x = 0; x <= board.settings.widthInches - selectedPiece.width; x += 1) { if (x === selectedPiece.x && y === selectedPiece.y) continue; const copy = { ...selectedPiece, id: crypto.randomUUID(), name: `${selectedPiece.name} copy`, x, y, locked: false }; if (placementResult(board, copy).ok) { command({ ...board, pieces: [...board.pieces, copy] }, `${copy.name} duplicated.`, [copy.id]); return; } } announce('No free space is available for a duplicate.', 'warning'); };
  const removeSelected = () => { if (!selectedIds.length) return; command({ ...board, pieces: board.pieces.filter((piece) => !selectedIds.includes(piece.id)) }, `${selectedIds.length} ${selectedIds.length === 1 ? 'object' : 'objects'} removed.`, []); };
  const join = () => { const result = joinStructures(board, selectedIds); if (!result.ok) { announce(`Cannot join structures: ${result.reason}.`, 'warning'); return; } command({ ...board, pieces: [...board.pieces.filter((piece) => !result.consumedIds.includes(piece.id)), result.piece] }, `${result.piece.name} joined into one footprint.`, [result.piece.id]); };
  const joinReason = selectedIds.length > 1 ? (() => { const result = joinStructures(board, selectedIds); return result.ok ? null : `Join unavailable: ${result.reason}.`; })() : null;
  const addFeature = (id: string, wall: WallSide, offset: number) => { const current = board.pieces.find((piece) => piece.id === id); if (!current) return; const next = addAccess(current, accessType, wall, offset); if (!next) { announce('That wall position cannot accept an overlapping access feature.', 'warning'); return; } command({ ...board, pieces: board.pieces.map((piece) => piece.id === id ? next : piece) }, `${accessType === 'door' ? 'Door' : 'Window'} added on the ${wall} wall.`, [id]); };
  const removeFeature = (id: string) => { if (!selectedPiece) return; const next = removeAccess(selectedPiece, id); command({ ...board, pieces: board.pieces.map((piece) => piece.id === next.id ? next : piece) }, 'Access feature removed.', [next.id]); };
  useEffect(() => { const key = (event: KeyboardEvent) => { if (event.target instanceof HTMLInputElement) return; const primary = event.ctrlKey || event.metaKey; if (primary && event.key.toLowerCase() === 'z') { event.preventDefault(); setHistory((value) => event.shiftKey ? redo(value) : undo(value)); } if (event.key === 'Delete' || event.key === 'Backspace') { if (selectedIds.length) { event.preventDefault(); removeSelected(); } } if (primary && event.key.toLowerCase() === 'd') { event.preventDefault(); duplicate(); } if (event.key.toLowerCase() === 'v') setMode('select'); if (event.key.toLowerCase() === 'b') setMode('build'); if (event.key.toLowerCase() === 'a' && selectedPiece?.structureDetails) setMode('access'); if (event.key === '1') setView('overhead'); if (event.key === '2') { setView('3d'); setPreset('isometric'); } if (event.key === '3') { setView('3d'); setPreset('perspective'); } if (event.key === '4') { setView('3d'); setPreset('front'); } }; window.addEventListener('keydown', key); return () => window.removeEventListener('keydown', key); }, [selectedIds, selectedPiece, history]);
  const loadStarter = () => { const next = starterBoard(); command(next, 'Starter layout loaded into the workspace.', []); };
  return <div className="app-shell"><header className="document-bar"><div className="brand"><span className="brand__mark" aria-hidden="true"><Crosshair size={17} /></span><span>Battle Builder</span><span className="brand__phase">Board planner</span></div><label className="board-name"><span className="sr-only">Board name</span><input value={board.name} onChange={(event) => mutate({ ...board, name: event.target.value })} /></label><span className="save-state"><Save size={14} />Draft changes tracked</span><div className="document-actions" aria-label="Document history"><IconButton label="Undo" tooltip="Undo (Ctrl+Z)" disabled={!history.past.length} onClick={() => setHistory(undo)}><Undo2 size={16} /></IconButton><IconButton label="Redo" tooltip="Redo (Ctrl+Shift+Z)" disabled={!history.future.length} onClick={() => setHistory(redo)}><Redo2 size={16} /></IconButton></div><Menu label="Board menu" trigger={<><span>Board</span><ChevronDown size={14} /></>}>{(close) => <><MenuItem onSelect={() => { close(); command(newBoard(), 'New board created.', []); }}> <FilePlus2 size={15} />New board</MenuItem><MenuItem onSelect={() => { close(); announce('Saved board library arrives in B08.'); }}><FolderOpen size={15} />Open…</MenuItem><MenuItem onSelect={() => { close(); duplicate(); }}><Copy size={15} />Duplicate</MenuItem><MenuItem onSelect={() => { close(); announce('Import is introduced in B08.'); }}><Upload size={15} />Import JSON…</MenuItem><MenuItem onSelect={() => { close(); toggleDrawer('setup'); }}><Settings2 size={15} />Board settings</MenuItem><MenuItem onSelect={() => { close(); setHelpOpen(true); }}><CircleHelp size={15} />Help & controls</MenuItem></>}</Menu></header><div className="workspace-layout"><nav className="activity-rail" aria-label="Workspace sections">{drawerMeta.map(({ id, label, icon: Icon }) => <IconButton key={id} label={label} tooltip={label} className={activeDrawer === id ? 'is-active' : undefined} aria-pressed={activeDrawer === id} onClick={() => toggleDrawer(id)}><Icon size={19} /></IconButton>)}</nav><main className="workspace" aria-label="Battle Builder workspace"><section className="canvas-stage" aria-label="Board workspace"><div className="canvas-stage__header"><div><span className="eyebrow">Precision board workspace</span><span className="board-dimensions">{board.settings.widthInches} × {board.settings.heightInches} in · fixed 1 in grid</span></div><div className="view-switcher" aria-label="Board view"><Button variant={view === 'overhead' ? 'accent' : 'quiet'} aria-pressed={view === 'overhead'} onClick={() => setView('overhead')}>Overhead</Button><Button variant={view === '3d' ? 'accent' : 'quiet'} aria-pressed={view === '3d'} onClick={() => setView('3d')}>3D planning</Button></div></div><div className="canvas-void"><div className="canvas-shelf" aria-label="Canvas controls"><div className="segmented"><Button variant={mode === 'neutral' ? 'accent' : 'quiet'} aria-pressed={mode === 'neutral'} onClick={() => setMode('neutral')}>Neutral</Button><Button variant={mode === 'select' ? 'accent' : 'quiet'} aria-pressed={mode === 'select'} onClick={() => setMode('select')}><MousePointer2 size={15} />Select</Button><Button variant={mode === 'build' ? 'accent' : 'quiet'} aria-pressed={mode === 'build'} onClick={() => { setMode('build'); setActiveDrawer('build'); }}><Hammer size={15} />Build</Button>{selectedPiece?.structureDetails && <Button variant={mode === 'access' ? 'accent' : 'quiet'} aria-pressed={mode === 'access'} onClick={() => setMode('access')}>Access</Button>}</div><span className="shelf-divider" /><Button variant={snap ? 'accent' : 'quiet'} aria-pressed={snap} onClick={() => setSnap((value) => !value)}><Grid3X3 size={15} />Snap</Button><Button variant="quiet" onClick={() => window.dispatchEvent(new KeyboardEvent('keydown', { key: 'f' }))}><Maximize2 size={15} />Fit</Button><Button variant="quiet" onClick={() => window.dispatchEvent(new KeyboardEvent('keydown', { key: '-' }))} aria-label="Zoom out"><Minus size={15} /></Button><output className="zoom-output" aria-label="Current zoom">1 in</output><Button variant="quiet" onClick={() => window.dispatchEvent(new KeyboardEvent('keydown', { key: '+' }))} aria-label="Zoom in"><Plus size={15} /></Button><div className="controls-wrap"><Button variant="quiet" aria-expanded={controlsOpen} onClick={() => setControlsOpen((value) => !value)}><CircleHelp size={15} />Controls</Button><Popover open={controlsOpen} label="Board control reference"><strong>Construction controls</strong><p>Build: drag to create · Select: click, Shift-click, or marquee · Move: drag or H/J/K/L · Rotate: R · Pan: middle or Shift-drag · Zoom: wheel. Access mode adds {accessType}s to exterior walls.</p></Popover></div></div>{view === 'overhead' ? <OverheadBoard board={board} mode={mode} buildKind={selectedCatalog} selectedIds={selectedIds} hoveredId={hoveredPieceId} onHoverPiece={setHoveredPieceId} onSelectionChange={setSelectedIds} onBuild={addBuild} onMove={move} onResize={resize} onRotate={(id, rotation) => patchPiece({ id, rotation: normalizeRotation(rotation) })} onAccess={addFeature} /> : <Suspense fallback={<p>Loading 3D planning view…</p>}><ThreeBoard board={board} preset={preset} selectedIds={selectedIds} onSelect={(id) => { setSelectedIds(id ? [id] : []); setMode(id ? 'select' : 'neutral'); }} /></Suspense>}{view === '3d' && <div className="preset-bar"><Button onClick={() => setPreset('top')}>1 Top</Button><Button onClick={() => setPreset('isometric')}>2 Iso</Button><Button onClick={() => setPreset('perspective')}>3 Perspective</Button><Button onClick={() => setPreset('front')}>4 Front</Button></div>}{board.pieces.length === 0 && <div className="zero-state"><div className="zero-state__glyph"><Crosshair size={32} /></div><p className="eyebrow">36 × 36 tactical surface</p><h1>Start building the battlefield.</h1><p>Choose terrain from the catalog, or begin from a balanced starter layout.</p><div className="zero-state__actions"><Button variant="accent" onClick={() => { setMode('build'); setActiveDrawer('build'); }}>Build / add terrain</Button><Button variant="quiet" onClick={loadStarter}>Load starter layout</Button></div></div>}</div></section><Drawer open={activeDrawer === 'board'} title="Board" onClose={() => setActiveDrawer(null)}><section className="drawer-section"><h3>{board.name}</h3><p>{board.settings.widthInches} × {board.settings.heightInches} inches · construction history ready</p></section><section className="drawer-section"><h3>Quick help</h3><p>Build terrain, then select to transform it. Access mode is available for buildings and ruins.</p><Button variant="quiet" onClick={() => setHelpOpen(true)}>Open controls reference</Button></section></Drawer><Drawer open={activeDrawer === 'build'} title="Build" onClose={() => setActiveDrawer(null)}><label className="search-field"><span className="sr-only">Search terrain catalog</span><input placeholder="Search terrain" /></label>{Object.entries(groupedCatalog).map(([group, items]) => <section className="drawer-section" key={group}><h3>{group}</h3>{items.map((item) => <button key={item.kind} className={`catalog-item ${selectedCatalog === item.kind ? 'is-selected' : ''}`} type="button" onClick={() => { setSelectedCatalog(item.kind); setMode('build'); setActiveDrawer(null); announce(`${item.name} armed. Drag on the board to set its inch footprint.`); }}><span><strong>{item.name}</strong><small>{item.width} × {item.height} in</small></span><span aria-hidden="true">+</span></button>)}</section>)}</Drawer><Drawer open={activeDrawer === 'layers'} title="Layers" onClose={() => setActiveDrawer(null)}><section className="drawer-section"><h3>Board objects</h3>{board.pieces.length ? board.pieces.map((piece) => <button className="catalog-item" key={piece.id} onClick={() => { setSelectedIds([piece.id]); setMode('select'); setActiveDrawer(null); }}><span><strong>{piece.name}</strong><small>{piece.kind} · {piece.width} × {piece.height} in</small></span></button>) : <p>No terrain pieces yet. Add terrain from Build.</p>}</section></Drawer><Drawer open={activeDrawer === 'setup'} title="Setup" onClose={() => setActiveDrawer(null)}><section className="drawer-section"><h3>Board dimensions</h3><p>{board.settings.widthInches} × {board.settings.heightInches} inches</p><p className="muted">The one-inch grid is permanent. Board dimension controls arrive in B07.</p></section><section className="drawer-section"><h3>Surface</h3><p className="muted">{board.settings.surface} surface · snap {snap ? 'on' : 'off'}</p></section></Drawer><SelectionInspector piece={selectedPiece} selectionCount={selectedIds.length} onClose={() => { setSelectedIds([]); setMode('neutral'); }} onChange={patchPiece} onDelete={removeSelected} onDuplicate={duplicate} onJoin={join} joinReason={joinReason} onAccessType={(type) => { setAccessType(type); setMode('access'); }} accessType={accessType} onRemoveAccess={removeFeature} /></main></div><Dialog open={helpOpen} title="Workspace controls" onClose={() => setHelpOpen(false)} actions={<Button variant="accent" onClick={() => setHelpOpen(false)}>Done</Button>}><p>Construction is intentionally direct: choose Build and drag an inch-snapped footprint, select objects for transforms, and use Access mode to put doors or windows on exterior walls.</p><dl className="help-list"><div><dt>Camera</dt><dd>Middle / Shift-drag pans, wheel zooms, F fits, and 1–4 changes views.</dd></div><div><dt>Keyboard</dt><dd>V Select, B Build, A Access, H/J/K/L move, R rotates, Ctrl+D duplicates, Delete removes.</dd></div></dl></Dialog><ToastRegion toasts={toasts} /></div>;
}
